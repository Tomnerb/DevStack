param(\n    [string]$Rootfs = ""\n)\n\n$ErrorActionPreference = "Stop"\n\nif (-not $Rootfs) {\n    $Rootfs = Join-Path $PSScriptRoot "..\\dist\\windows-wsl\\dockiva-wsl-rootfs.tar"\n}\n\n$Rootfs = [System.IO.Path]::GetFullPath($Rootfs)\n\nif (-not (Test-Path $Rootfs)) {\n    throw "Rootfs not found: $Rootfs"\n}\n\n$InstallDir = Join-Path $env:LOCALAPPDATA "Dockiva\\wsl"\nNew-Item -ItemType Directory -Force -Path $InstallDir | Out-Null\n\n$distros = (wsl.exe --list --quiet) -replace "`0", ""\n\nif ($distros -contains "Dockiva") {\n    Write-Host "Dockiva WSL2 distro already exists."\n    exit 0\n}\n\nwsl.exe --import Dockiva $InstallDir $Rootfs --version 2\nwsl.exe -d Dockiva -u root -- /usr/local/sbin/dockiva-start\n\nWrite-Host "Dedicated Dockiva WSL2 distro installed."\n